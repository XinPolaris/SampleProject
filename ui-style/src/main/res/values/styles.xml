<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 基础主题 -->
    <!-- 按钮样式 -->
    <style name="AppButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/btn_orange_gradient</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/font_size_default</item>
        <item name="android:paddingLeft">@dimen/padding_horizontal</item>
        <item name="android:paddingRight">@dimen/padding_horizontal</item>
    </style>

    <!-- 输入框样式 -->
    <style name="AppEditText" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/bg_edittext_default</item>
        <item name="android:textColor">@color/dark_gray</item>
        <item name="android:textColorHint">@color/hint_color</item>
        <item name="android:textSize">@dimen/font_size_default</item>
        <item name="android:padding">@dimen/padding_vertical</item>
    </style>

</resources>